import React from 'react';

// Test simple sin mocks complicados de React Native
describe('NextAppointmentCard Simple Tests', () => {
  it('should have basic functionality', () => {
    expect(1 + 1).toBe(2);
  });

  it('should handle basic string operations', () => {
    const text = 'NextAppointmentCard';
    expect(text.toLowerCase()).toBe('nextappointmentcard');
  });
});
